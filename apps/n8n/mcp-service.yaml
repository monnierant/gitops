apiVersion: v1
kind: Service
metadata:
  name: mcpgateway-service
  namespace: n8n
spec:
  selector:
    app: mcpgateway
  ports:
    - protocol: TCP
      port: 8811
      targetPort: 8811
